<?xml version="1.0" encoding="UTF-8"?>
<mvvm:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="Fourplaces.Views.Inscription"
             xmlns:mvvm="clr-namespace:Storm.Mvvm.Forms;assembly=Storm.Mvvm.Forms"
             >    
    
    <ContentPage.Content>
        <StackLayout>
        
            <Label Text="S'inscrire" HorizontalTextAlignment="Center"/>
            <Entry Text="{Binding EMAIL}" Placeholder="Email" />
            <Entry Text="{Binding FNAME}" Placeholder="Prénom" />
            <Entry Text="{Binding LNAME}" Placeholder="Nom" />
            <Entry Text="{Binding MDP}" Placeholder="Mot de passe" />
            <Button Text="OK" Command="{Binding SEND}"/>
            <Label Text="{Binding EXCEPTION}" TextColor="Red"/>
 
            
        </StackLayout>
        
    </ContentPage.Content>
    
</mvvm:BaseContentPage>
